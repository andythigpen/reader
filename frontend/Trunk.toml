[build]
target = "index.html"
dist = "../dist"
release = false

[tools]
wasm_opt = "version_131"

[[hooks]]
stage = "pre_build"
command = "tailwindcss"
command_arguments = ["-i", "input.css", "-o", "app.css"]
