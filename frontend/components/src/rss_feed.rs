use yew::prelude::*;
use yew::Properties;
use yewdux::prelude::*;

// #[derive(Properties, PartialEq, Clone)]
// pub struct Props {
//     pub id: usize,
// }

#[function_component(RssFeed)]
pub fn rss_feed(/* &Props { model }: &Props */) -> Html {
    // let article = &store.articles[id];
    html! {
        <>
            {"TODO"}
        </>
    }
}
